#pragma once
// ---------------------------------------------------------------------------
// SatelliteDataset.h - internal header (kept for backward compatibility).
// All public types (Triangle, JointConfig, SatelliteDataset) are now
// defined in SRPLibrary.h, which is the single header distributed to users.
// ---------------------------------------------------------------------------
#include "SRPLibrary.h"
